L'interface on veut afficher le graph résultat des workflow à l'aide de d3.js
3 type de page: recherche, résultat, résultat détaillé.

Décider de quelle manière les requêtes vont être entré par l'utilisateur. (Un formulaire ou une syntaxe de requête)

Trouver les paramêtres de la requête (terme d'entré, de sortie, profondeur max, profondeur min, nbr de valeur de retour) peut-être noeud
intermediaire obligatoire. Dans l'état la requête peut-être enrichie en fonction des entrées de l'utilisateur.

Décider ce que renvoie la fonction de requête cypher

Ecrire des fonctions de tri des résultat de workflow, idée de fonction en tri: (Workflow les plus cours en premier, workflow qui cite le plus de
terme proche de l'ontologie recherché)

Comment chercher un workflow avec un terme de l'ontologie avec des outils pas forcement avec ce terme 

Trouver un équilibre sur les informations à renvoyer à l'interface pour pas faire trop de requêtes, mais aussi pour ne pas envoyer trop de données (tailles du json)
